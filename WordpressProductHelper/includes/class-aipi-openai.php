<?php
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}

/**
 * OpenAI client placeholder.
 *
 * Must:
 * - accept model and API key from settings
 * - send structured request
 * - handle HTTP errors, malformed JSON, and empty output
 */
class AIPI_OpenAI {
    public function generate_draft( array $payload ) {
        // TODO
    }
}
